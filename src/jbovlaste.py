#!/usr/bin/env python3.5
from lxml.html import parse
import os
vlas = parse(os.environ["JBOVLASTE"]).getroot()

class Definition:
    def __init__(self, word):
        self.word = word
    def enWord(n):
        v = Defintion()


if __name__ == '__main__':
    main()
